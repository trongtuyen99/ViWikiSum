keyword: Chồng hàm
url: https://www.stdio.vn/modern-cpp/nap-chong-ham-function-overloading-AjcgZ1
content:
Trong trường hợp này, để có được kết quả mong muốn phải viết hàm mới để tính max của 2 số thực. Tương tự cũng sẽ có double hay char nếu như cần sử dụng kiểu dữ liệu này. Như vậy, sẽ có nhiều hàm với các tên gọi khác nhau. Việc sử dụng tên như vậy sẽ gây bất lợi về mặt tổ chức cho người lập trình khi gọi hàm. Nạp chồng hàm ra đời để giải quyết vấn đề trên. Nạp chồng hàm là một tính năng được hỗ trợ thêm trong C++, tính năng này không tồn tại trong C. Tính năng này cho phép sử dụng cùng một tên gọi cho các hàm giống nhau (có cùng mục đích). Nhưng khác nhau về kiểu dữ liệu tham số hoặc số lượng tham số. thì trình biên dịch sẽ tự động nhận dạng và tạo ra lời gọi hàm đến hàm cần thiết dựa vào kiểu dự liệu trong tham số truyền vào hàm. Ngoài ra còn 1 trường hợp khác cũng cho phép khai báo và định nghĩa 2 hàm cùng tên, đó là số lượng tham số của chúng phải khác nhau. Trong trường hợp này khi gọi hàm, chương trình sẽ dựa vào số lượng tham số khi gọi để quyết định gọi hàm tương ứng. Có thể nạp chồng hàm nếu như kiểu của tham số trong hàm phải khác nhau hoặc có số lượng các tham số khác nhau.
